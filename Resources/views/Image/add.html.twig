{{ form_start(form) }}
<p class="fileHelper"></p>
    {{ form_row(form.file) }}
<div id="fileError"></div>
    {{ form_row(form.name) }}
    {{ form_row(form.alt) }}
    {{ form_rest(form) }}
{{ form_end(form) }}